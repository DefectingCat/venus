import clsx from 'clsx';

const SystemTray = () => {
  return (
    <>
      <div
        className={clsx(
          'rounded-lg overflow-hidden bg-white',
          'h-full flex p-4',
        )}
      >
        123
      </div>
    </>
  );
};

export default SystemTray;
